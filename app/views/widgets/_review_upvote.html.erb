<% if current_user && review.user_id != current_user.id %>
<div class="review-upvote-container group">
  <div class="upvote-question"><em>Was this review ...?</em></div>
  <% votes = review.vote_count %>
  <form
    class="review-vote"
    action="<%= toggle_vote_review_url(review.id) %>"
    method="post"
    data-remote="true">

    <% Vote.all.each do |vote| %>
      <% voted_id = current_user.get_vote(review, vote.id) %>
      <% display_name = (votes[vote.id] ? "#{vote.name} ( #{votes[vote.id]} )" : vote.name ) %>

      <button type="submit" class="vote_<%= vote.id %> <%= 'voted' if voted_id %>" name="vote_id" value="<%= vote.id %>"><%= display_name %></button>
    <% end %>
  </form>

  <% if current_user && review.user_id == current_user.id %>
    <a href="<%= edit_review_url(review.id) %>">edit</a>
  <% end %>
</div>
<% end %>







