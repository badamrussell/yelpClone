<fieldset>
  <h3><%= title %></h3>

  <% data.each_with_index do |d,i| %>
    <input type="checkbox" name="<%= prefix %>[<%= name %>][]" value="<%= d.id %>" id="<%= prefix %>_<%= name %>"><%= d.name %>
  <% end %>
</fieldset>


